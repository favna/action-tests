name: Testing Actions

on:
  push:

jobs:
  print_context:
    runs-on: ubuntu-latest
    env:
      DOCKER_TARGET: ${{ github.ref_name == 'main' ||  format('pr-{0}', github.ref_name)  }}
    steps:
      - run: echo $DOCKER_TARGET

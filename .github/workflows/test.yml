name: Sample Workflow

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  main_job:
    name: Test Job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Project
        uses: actions/checkout@v2
      - name: Do stuff
        run: |
          nc -h
